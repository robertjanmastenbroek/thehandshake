FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir requests

# Copy the Moltbook agent
COPY moltbook_agent.py .
COPY moltbook_credentials.json /root/.config/moltbook/credentials.json

# Run the autonomous agent
CMD ["python3", "moltbook_agent.py"]
